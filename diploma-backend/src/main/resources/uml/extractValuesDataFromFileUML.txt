@startuml
start
:Открыть файл сырых значений;
:Создать пустую карту для хранения данных;
:Прочитать первую строку файла (заголовки);
repeat :Итерация по заголовкам;
  :Добавить новый элемент в карту с ключом - индексом заголовка и значением - именем заголовка и пустым списком значений;
repeat while (Есть еще заголовки)
repeat :Итерация по строкам файла;
  :Прочитать значение даты и времени из текущей строки;
  repeat :Итерация по ячейкам текущей строки;
    :Добавить новое значение в список значений соответствующего элемента карты;
  repeat while (Есть еще ячейки в строке)
repeat while (Есть еще строки в файле)
:Преобразовать карту в список объектов промежуточной структуры данных;
:Вернуть результат;
stop
@enduml