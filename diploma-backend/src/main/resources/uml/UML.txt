@startuml
start
:Создать пустой список для результата;
repeat :Итерация по списку значений;
  :Получить значения и дельты для текущего индекса;
  :Создать новый контейнер для нечетких чисел для текущего индекса;
  repeat :Итерация по значениям;
    :Расчет границ для каждого значения;
    :Добавить рассчитанное нечеткое число в контейнер для нечетких чисел;
  repeat while (Есть еще значения)
  :Добавить контейнер для нечетких чисел в список результатов;
repeat while (Есть еще элементы в списке значений)
:Вернуть результат;
stop
@enduml